<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.seulobinho">

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_main4"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".FragmentoExames.NovoExameActivity">
        </activity>
        <activity android:name=".FragmentConsulta.NovaConsultaActivity">
        </activity>
        <activity android:name=".FragmentRemedio.NovoRemedioActivity">
        </activity>
        <activity android:name=".cadastrarNovoItem">
        </activity>
        <activity android:name=".CadasUserActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>